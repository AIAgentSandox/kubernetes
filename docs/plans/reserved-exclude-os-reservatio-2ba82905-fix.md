Do deeper investigation and write findings as part of a plan. List all options of source for this information and it's portability.

For implementation built-into the kubelet, because of backward compatibility question, the new accounting must be enabled using separate configuration flag. Design for this flag if there is a portable solution.

For the external implementation on COS and Ubuntu, only do the absolute minimum of planning how exactly the program will read this information, the integration with bootstrap is outside of the scope of this task.